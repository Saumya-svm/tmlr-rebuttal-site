{
  "name": "tmlr-rebuttal-site",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "mkdir -p _site && node build.mjs src _site/index.html --standalone && cp robots.txt _site/"
  },
  "dependencies": {
    "marked": "^12.0.2",
    "katex": "^0.16.9"
  }
}
